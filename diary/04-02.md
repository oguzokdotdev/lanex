Split `../src/main.c` into `../src/vga.c` with `../include/vga.h` and `../src/tty.c` with `../include/tty.h`

Created the `../diary` directory and this note
Next up is creating INTS and a keyboard driver (I will die)
